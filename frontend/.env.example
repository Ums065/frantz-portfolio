# Frontend environment template (Vite). Copy to `.env`.
# Variables MUST be prefixed with VITE_ to be exposed to the app.

# API base path. Dev: "/api" (proxied to PHP by Vite).
# Production: full URL of the deployed API, e.g. https://frantzcoutard.com/api
VITE_API_BASE=/api
