# Frontend environment template (Vite). Copy to `.env`.
# Variables MUST be prefixed with VITE_ to be exposed to the app.

# API base path. Use a relative path so it works at the site root and in a
# subfolder deployment such as /frantz-portfolio/.
VITE_API_BASE=api
